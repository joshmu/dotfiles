dotbot/
!dotbot/README.md
BrewfileOld*
nvim.bak
**/*backups/**
mcp.creds.json

# we ignore it but still symlink from our dotfiles folder
claude_desktop_config.json
cursor/mcp.json
.claude-trace
.claude

# private
cursor/rules/tech-stack-info.mdc

.env
config.json
!caveman/config.json

# ignore my mcp registry
mcp-registry.json

# dependencies
node_modules/
bun.lock

# bun build artifacts
*.bun-build

# per-machine shell env override (e.g. AGENT_OBSERVABILITY_PATH)
.zshenv.local
