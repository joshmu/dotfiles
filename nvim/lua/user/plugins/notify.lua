return {
  "nvim-notify",
  opts = {
    -- stages = "slide",
    -- timeout = 3000, -- tried to use this and it didn't work...
    -- max_height = function() return math.floor(vim.o.lines * 0.75) end,
    -- max_width = function() return math.floor(vim.o.columns * 0.75) end,
  },
}
