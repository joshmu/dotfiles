{
  "name": "bitbucket-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Bitbucket CLI tools",
  "keywords": [
    "bitbucket",
    "cli"
  ],
  "type": "module",
  "scripts": {
    "pr:create": "bun bitbucket-pr-create.ts",
    "pr:update": "bun bitbucket-pr-update.ts",
    "pr:list": "bun bitbucket-pr-list.ts",
    "repos:list": "bun bitbucket-repos-list.ts",
    "setup": "cp config.json.example config.json && echo 'Please edit config.json with your credentials'"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
